# Implementation Plans

Generated by the improve skill on 2026-07-04 (commit `99fab46`).
Execute in the order below unless the dependency notes say otherwise.
Each executor: read the plan fully before starting, honor its STOP conditions,
and update your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | Mark recording-pipeline vars `nonisolated(unsafe)` | P1 | S | — | DONE |
| 002  | Fix data race in `beginLongExposureCapture` | P1 | S | — | DONE |
| 003  | Stop recording cleanly on AVCaptureSession interruption | P1 | M | 001 | DONE |
| 004  | Check `assetWriter.status` in `appendVideoFrame`, surface errors to UI | P2 | S | 001 | DONE |
| 005  | Fix TOCTOU in `stopRecording` — keep writer non-nil until `finishWriting` completes | P2 | S | 001 | DONE |
| 006  | Fix `availableZoomFactors` for physical camera optical stops | P2 | S | — | DONE |
| 007  | Throttle audio level updates to ~10fps | P3 | S | — | DONE |

Status values: `TODO` | `IN PROGRESS` | `DONE` | `BLOCKED (reason)` | `REJECTED (reason)`

## Dependency notes

- 003 requires 001: the interruption observer reads `isWaitingToRecord` and `assetWriter` on sessionQueue; they must be `nonisolated(unsafe)` first.
- 004 requires 001: `appendVideoFrame` reads `assetWriter` on sessionQueue.
- 005 requires 001: adds `isFinishingRecording` which must also be `nonisolated(unsafe)`.
- 001 and 002 are independent of each other; run in parallel if desired.
- 006 and 007 are fully independent; run any time.

## Findings considered and rejected

- **`blendFrames` reusing a mutable `CIFilter` in a loop**: correct — CI lazy evaluation captures input state per `outputImage` call. Not a bug.
- **Double `startSession` during splash**: second call hits `guard !session.isRunning else { return }`. Harmless.
- **`LUTLoader.parseCube` O(N) string parsing**: cached after first parse; cost is negligible at ~36K lines/style, called once per style change.
- **`stopRecording` called when not recording (mode teardown)**: hits `guard let writer = assetWriter else { return }`. Harmless no-op.
- **frameCount integer overflow**: at 60fps, `Int.max` is reached in ~4.9 billion years. Not a concern.
